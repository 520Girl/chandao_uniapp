---
description: 这个是git提交规则
globs: ["*"]
alwaysApply: false
---
---
description：Git提交和版本控制规范
globs: [".git/*","*.git"]
alwaysApply: false
---

# Git 版本控制规范

## 规则描述
此规则文件定义了项目的 Git 版本控制规范，包括提交信息格式、分支管理、代码审查等最佳实践。

## 自动附件
- 适用于所有代码文件
- 适用于 Git 相关操作

## Git 提交规范

### 提交信息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型说明
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

### 示例
```
feat(dingtalk): 添加群发消息功能

- 实现钉钉群发消息API
- 添加消息发送日志记录
- 支持批量用户选择

Closes #123
```

## 分支管理规范

### 分支命名
- `main`: 主分支，用于生产环境
- `develop`: 开发分支，用于集成测试
- `feature/*`: 功能分支，用于新功能开发
- `hotfix/*`: 热修复分支，用于紧急修复
- `release/*`: 发布分支，用于版本发布

### 工作流程
1. 从 `develop` 分支创建 `feature` 分支
2. 在 `feature` 分支上开发功能
3. 完成后提交 Pull Request 到 `develop` 分支
4. 代码审查通过后合并
5. 定期从 `develop` 分支创建 `release` 分支
6. 测试通过后合并到 `main` 分支

## 代码审查规范

### 审查要点
- 代码质量和可读性
- 功能完整性和正确性
- 安全性考虑
- 性能优化
- 测试覆盖率

### 审查流程
1. 提交 Pull Request
2. 至少一名团队成员审查
3. 解决审查意见
4. 审查通过后合并

## 版本标签规范

### 标签格式
```
v<major>.<minor>.<patch>
```

### 版本号规则
- `major`: 重大版本更新，可能不兼容
- `minor`: 功能更新，向后兼容
- `patch`: 补丁更新，修复bug

### 示例
```
v1.0.0 - 初始版本
v1.1.0 - 添加钉钉机器人功能
v1.1.1 - 修复消息发送bug
```